package com.reto.backend.repository;

import com.reto.backend.model.entity.MaquinaEscribir;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaquinaEscribirRepository extends JpaRepository<MaquinaEscribir, Long> {
}
